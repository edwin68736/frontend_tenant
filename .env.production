# URL del backend en producción. Obligatorio para que el login y la API no apunten a localhost.
VITE_API_URL=https://api.tukifac.com

# En producción el slug del tenant se detecta del subdominio (ej: demo.app.tukifac.cloud → demo).
# No hace falta definir VITE_TENANT_SLUG.
